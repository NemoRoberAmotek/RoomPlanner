import wood from "../assets/wood.svg";
import tile from "../assets/tile.svg";

const getTextures = () => {
  const textures = {
    none: "none",
    wood,
    tile,
  };

  return { textures };
};

export default getTextures;
